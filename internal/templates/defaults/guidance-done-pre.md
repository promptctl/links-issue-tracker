Double check the ticket to verify it's truly complete. Consider running a subagent to verify your work for any logic errors, missing edge cases, unstable workarounds, deferred implementations, or missing requirements. If you run a subagent, tell them to run 'lit show <id>' so they can read the ticket themselves. Ask yourself (not the user): if the user reviewed my work thoroughly, would they ask me to change anything? Do not rerun the tests. Always commit your changes and try to leave a clean working dir. When the work is truly complete, run `lit done <id> --apply` to close the ticket.
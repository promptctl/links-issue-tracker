# Changelog

All notable changes to `links-issue-tracker` (the `lit` CLI) are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- `go run ./tools/licenses -graph` classifies every module in the go.mod build list — the 588 `go list -m all` then resolved — instead of only the 158 the compiler then linked into `./cmd/lit`. An auditor reads go.mod and go.sum, and until now nothing in CI had ever looked at the difference: 330 of those modules had never been downloaded to this machine at all, so their license posture was unmeasured rather than clean, and a dependency bump could add a GPL coordinate to go.sum with every check staying green. The mode resolves the graph itself, running `go mod download all` before `go list` so "every module has a local copy" is something the tool guarantees rather than something the caller has to remember — a run against a completely empty module cache produces byte-identical output to a warm one (verified; it pulls 3.4 GB to do it). It shares the classifier and `policy.json` with the existing `-check` gate through one extracted accept/reject rule, so "permissive" cannot come to mean two different things in the two modes, and it writes no artifacts: the module graph must never leak into the SBOM or the attribution bundle, which assert what is *in* the binary. Licenses are found at any depth and by two routes — a file whose name says it is one, and any file inside a directory named `licenses/`. The second route is not redundant: `github.com/golang/freetype`'s root LICENSE is a pointer document that no classifier can match, and its actual GPL-2.0 text sits at `licenses/gpl.txt`, a name no license-shaped filename pattern reaches. Findings are grouped by what a reader must do about them — a module's own root grant (what a scanner resolving `module@version` reports) separately from texts nested in its tree (what a file-walking scanner flags), unclassifiable files in their own section, and modules under a `replace` directive listed whether or not their license is permissive, because the go tool reports the *original* path and version against the *replacement's* directory and that divergence is itself the finding.
- Degraded sync state now reaches the OWNER out-of-band, not just whichever agent runs the next command. A new `sync.owner_notify_cmd` config setting (empty by default) names a shell command — e.g. a `curl` to an ntfy topic — that lit runs at detection time, with the event's facts in `LIT_NOTIFY_KIND`/`LIT_NOTIFY_SUMMARY`/`LIT_NOTIFY_REMOTE`/`LIT_NOTIFY_BRANCH`/`LIT_NOTIFY_REPO`, whenever a surface detects a real divergence (no common ancestor, a reconcile that could not converge, a held prose conflict) or a push attempt completes failed — the inline auto-receive, `lit sync pull`, the explicit reconcile, `lit doctor`'s persistent-divergence escalation, and the one shared push path all feed it. Notifications de-duplicate per episode via per-kind `owner-notify.<kind>.last` markers: the first detection fires immediately, the same persisting condition re-pings at most daily, and a resolved condition (a landed push, a converged reconcile) clears its marker so the next occurrence fires immediately again. A failed hook is loud on stderr, recorded in `traces/sync/`, and retried on the next detection rather than silently marking the owner informed; `LIT_DISABLE_AUTO_SYNC=1` suppresses the hook along with the rest of automatic sync. In the links-sync-pgct field incident the owner learned of a week-old fork only because duplicated work happened to get re-ordered and a session went digging — this is the channel that makes the fork the owner's news the day it forms.
- `lit sync reconcile take local|remote` — the resolution that permanently discards one side's unique issues — now refuses to run without the owner's explicit approval. Run bare it exits 5 with a refusal block naming exactly which issues the take would destroy (the both-sides inventory), pointing at the approval-free alternative (`lit sync reconcile combine`, the union that keeps every issue and stays agent-runnable), and carrying a one-time `--owner-approved` token; only a re-run with that token performs the destruction. The token is a digest of both heads and the chosen side, checked inside the store under the same commit lock that mutates — so any commit landing on either side after it was issued, or presenting a take-local token to take-remote, voids it and re-mints a fresh one for the fork as it now stands (the prose-resolve fingerprint device at whole-backlog scope). Every gate refusal and every approved take is durably traced. This closes the incident's near-miss: an agent alone at a no-common-ancestor fork was offered 'take local' / 'take remote' — two destroy-a-side options resolved safely only by luck; now the party who can lose the work is the party who authorizes the loss.
- The push-failure staleness banner now reaches sessions that only run mutating commands. Every push attempt — the on-change background mirror and the explicit `lit sync push` alike — records how it ended (pushed, skipped and why, or failed and why) in a `push-outcome.last` marker in the workspace's storage dir, written by the one shared push path so the marker and the durable `traces/sync/` record can't disagree. Every mutating command (`new`, `start`, `update`, `close`, `comment`, `rank`, …) then prints a `sync: automatic push … is FAILING` banner after its own output whenever the last attempt failed, plus the existing stale-fetch warning — wired once at the command dispatch seam, so a mutating command added tomorrow is covered without remembering any call. Previously the banner was wired into exactly three read commands (`lit backlog`/`next`/`show`), so an agent session chaining `new → update → close` against a dead remote — a completely ordinary agentic pattern — saw nothing on any command it actually ran while the failures piled up in `mirror.log`. The mutating-side banner deliberately keys on "the last push attempt failed", not the read commands' ahead-count: a mutating command is always momentarily ahead at its own end (its mirror pushes only after the process exits), and an ahead-based banner there would warn on every healthy mutation and train its readers to ignore it. The read commands lead with the same failure line when it applies (the ahead-count line shows the accumulating effect; this names the cause), and `lit doctor` now surfaces the failure's reason plus the mirror worker's log (`mirror.log` path and last-write age) — previously a durable failure sink no command ever pointed at.
- `lit backlog`, `lit next`, and `lit show` now print a `sync:` warning line up front when unpushed/unfetched state has drifted, instead of that only surfacing on request via `lit doctor`. Two independent conditions trigger it: local is ahead of the remote by unpushed changes (`lit sync push` to fix), or a fetch against the remote hasn't succeeded in over 24h (`lit sync fetch` to fix) — tracked by a new `fetch-success.last` marker in the workspace's storage dir, stamped by every real successful fetch (`lit sync fetch`/`pull`, the reconcile command's pre-reconcile fetch, and the inline auto-receive), distinct from the existing debounce marker so a remote that keeps failing every fetch attempt still reads as stale. A workspace with no remote configured stays silent for both conditions; a workspace already mid-divergence (which already gets the heavier `SyncFailure` escalation from the reconcile machinery) suppresses only the unpushed-ahead condition — the stale-fetch condition is independent and still fires. This closes the other half of the links-sync-pgct field incident: 25 local changes and a fresh, never-synced clone both sat unnoticed for days because nothing surfaced drift on the commands actually run day to day.
- `just build` now stamps `internal/version.{Commit,Date}` via `-ldflags`, computed locally (`git rev-parse --short HEAD`, the local clock — no network call) through the new `scripts/version-ldflags.sh`, which `scripts/install.sh`'s source mode now shares too. `lit version` reports the real commit and how long ago the binary was built instead of unconditionally "commit unknown, built unknown", and warns when a binary is older than `internal/version.StaleBuildThreshold` (7 days) — the previous silence made a stale local binary look identical to a fresh one, which is the suspected root cause of the links-sync-pgct field incident. `just build` deliberately leaves `Version` unstamped so ordinary local builds stay `IsDev == true`, preserving the guard that keeps a stray dev build from overwriting a real release's downgrade stamp.
- Dev-build status now surfaces proactively instead of only on request via `lit version`. `lit doctor` prints a `build:` line naming whether the running binary is a dev or release build, and — for a dev build — how long ago it was built, flagged `STALE` past `internal/version.StaleBuildThreshold`. `lit init`'s phase-boundary output names the same status the moment it commits to a "start fresh" decision (no remote backlog to adopt) or reports an adopt failure or success. Every sync-failure block (`lit sync pull`/`push`/`reconcile`, and `lit doctor`'s persistent-divergence exit) names it too, since a stale local binary silently missing a landed fix was the suspected root cause of the field incident this epic exists to prevent — naming the build status at the moment of failure turns a mysterious divergence into a five-minute diagnosis instead of a second `lit version` round trip.
- Every sync/init decision — `lit init`'s remote-adopt outcome and each `lit sync fetch`/`pull`/`push`/`reconcile` action and its result, including the on-change push mirror and the inline receive/reconcile that runs after an ordinary command — is now recorded unconditionally as a durable JSON record under `traces/sync/`, alongside the existing `traces/automation/` `lit doctor` already points at via `traces_dir`, using the same `internal/trace` writer `internal/workflows` already uses for firing traces. Previously the only durable trace (`traces/automation/`) fired solely when `LNKS_AUTOMATION_TRIGGER` was set, so a directly-run interactive `lit init` or `lit sync` command — and the inline auto-receive/reconcile that runs after nearly every ordinary command, since nothing sets that trigger for it — left no record anywhere once the process exited. This was the missing evidence trail behind the links-sync-pgct field incident: the divergence was found only because an agent happened to go digging mid-session. Each record's `trigger` field is empty for an interactive occasion and names the trigger for an automated one, so the new unconditional trace and the existing automation-gated trace can never disagree about what fired a given occasion; the automation-gated trace and its consumers are unchanged.

### Changed

- lit's lock discipline is now declared in one place — `internal/filelock`'s package doc — instead of existing only as consistent-but-unwritten practice: owner exclusion is an flock through `filelock.Acquire`, with "is the owner still alive" answered only by exclusive acquisition (never an mtime, a PID probe, or a wall-clock threshold) — the declared rule, not yet a universal fact: the commit lock's `O_EXCL` create with PID/mtime staleness and the mirror-pending marker's age-out predate it and are named in the doc as being rebuilt onto it (links-locking-il18.2/.4); the acquisition order, outermost to innermost, is workspace → engine → Dolt's own `.dolt/noms/LOCK` → commit → beacon (the sync-push lock sits outside those slots — outermost in practice, but only ever taken as a non-blocking probe, so nothing can wait on it); and lock files sit beside the dolt directory so a `lit snapshots restore` that rotates the directory cannot move them out from under acquirers, with the two placement exceptions (the snapshot producer beacon inside `snapshots/`, and the adopt-pending marker — a condemnation record, not a lock — inside the dolt root) each stating its reason where its path is minted. Writing it down surfaced one fact no call site shows: Dolt's LOCK is acquired by the embedded driver when a write engine opens — lazily, at first SQL use — and held for the Store's whole lifetime, so taking it while holding the commit lock inverts the order against every live write Store and can deadlock. The doc records the rule (take it before commit or not at all) and the tolerated deviation — the long-lived Store engine acquires LOCK under the commit lock, first at Open's migrate (`ensureDoltDatabase`'s transient bootstrap engines have already taken and released it by then) and again on GC-contention reconnects — with the reason it cannot cycle today, which the engine-lock retirement (links-locking-il18.3) now owns keeping true. No behavior changes; this is the declaration the rest of the links-locking-il18 epic lands against.

- `lit` no longer links `github.com/hashicorp/golang-lru` — either major version, both MPL-2.0 — anywhere. The two coordinates arrived through exactly eight files lit never wrote: seven in Dolt (`store/nbs`, `libraries/doltcore/doltdb`, `libraries/doltcore/remotestorage`) importing v2, and go-mysql-server's `sql/cache.go`, the single file that put v1 into the binary. Both forks now import `github.com/promptctl/primitives` instead — `lru` (generic) in Dolt, `lruany` in go-mysql-server — a new org-owned, MIT, stdlib-only module whose cache packages were written through the clean-room protocol in `design-docs/clean-room-reimplementation.md`: four sequential agents, only a behavioral spec and then reviewed tickets crossing the wall, the implementer sandboxed away from the original's source, and the whole record — what was read, the sandbox verdicts, every ruling and known divergence — in that module's `PROVENANCE.md`, audited before integration by an agent that was never behind the wall. In Dolt the swap is an import rewrite alone, because the replacement ships the replaced method sets exactly; in go-mysql-server it additionally touches the two `lru.New` call sites, the one behavioral change: they discarded the constructor's error and now panic at construction — at the call that owns the bad size — instead of failing at first use, far from the cause. Performance was measured, not assumed, at one pinned shape on both sides (8 goroutines, one shared instance, 90% Get / 10% Add, capacity 1024 against a 4096-key working set, medians of three 3s runs): the original measured 204.5 ns/op (`Cache`) and 263.0 ns/op (`TwoQueueCache`); the replacement, fetched as the published `v0.1.0` the build actually links, 190.4 and 199.6 — faster on both types, zero allocations per operation on both sides. The vendored `dolthub/driver` module and the Dolt fork each gained a `replace` mirroring the top-level fork pins so that, resolved standalone, neither re-records a golang-lru coordinate through an upstream go.mod; inside lit's build those replaces are inert. The freshly generated `LICENSE-REPORT.md` and SBOM now carry no MPL-2.0 row at all — 155 components, down from 156 on the then-current master (two coordinates out, one in; that 156 already reflected the go-sql-driver removal below, which merged first, and the buzhash cut below later took the count to 154) — and `go run ./tools/licenses -graph` finds neither coordinate in the module graph. What remains of the name in go.sum is two `/go.mod`-only hash lines (v0.5.0/v0.5.1, ancient `go.opencensus.io`-era graph ancestry): they carry no source hash and ship no code — a milder residue than the freetype entry, which does carry a source hash and ships its GPL text, though both belong to the same graph-cleanup ticket.
- `lit` no longer depends on `github.com/go-sql-driver/mysql` (MPL-2.0), in any module, at build time or test time. The coordinate was carrying exactly two fields — a MySQL server error code and a message — across a package boundary between two modules lit already owns on both ends: the vendored `github.com/dolthub/driver` constructed that project's `*MySQLError` in `translateError`, and `internal/store` asserted the same type to recognise error 1146 and read a pre-goose commit as schema 0. The driver now defines its own `MySQLError` and the store matches on that, so the removal is a single change with both halves in it — replacing only the consumer would have left `errors.As` silently failing to match, `isMissingTableError` returning false for a genuinely missing table, and nothing failing to compile. The type was written from the MySQL protocol and from what those two call sites actually need, not from go-sql-driver's source: `design-docs/clean-room-reimplementation.md` makes rewrite this epic's default, and a shape partly dictated by a wire protocol is still not ours to copy out of an MPL-2.0 file. It carries no SQL state, because nothing produces one and nothing reads one. Its rendering, `Error <code>: <message>`, was not a free choice — seven existing assertions in the driver's own smoke tests already pinned it, and they pass unchanged. Two test-only dependencies went with it, since a test-only requirement is still a line in `go.mod` and still a row an auditor reads: upstream's MySQL-comparison mode in `smoke_test.go` (a hardcoded `runTestsAgainstMySQL = false` plus seven `if`/`else` sites and a swap to a `localhost:3306` server this repo never provisions — dead in every run, and the `else` arms unreachable), and `gorm_test.go`, which was the sole surviving path to the coordinate via `gorm.io/driver/mysql` and tested an ORM integration lit uses nowhere. Every other test in the driver's suite remains and still runs. MPL-2.0's count in the link closure drops from 3 to 2 — the remaining two were the golang-lru coordinates, whose removal (recorded above) later took it to zero. The coordinate's full hash rows do remain in both go.sums, kept for the dependency test graph — the dolt fork's binlogreplication tests import the driver — so an auditor reading go.sum still meets the name there: not linked, not buildable from lit, and owned by the graph-cleanup ticket alongside freetype.
- `lit` no longer depends on buzhash under either of its two coordinates — `github.com/kch42/buzhash` (linked into the binary) and `github.com/silvasur/buzhash` (go.mod/go.sum only), the same 2016 code published under the author's account before and after a rename, both carrying a hand-written, profane WTFPL variant that no classifier matches, so both classified as Unknown: under this epic's bar the worst row a report can hold, because an auditor cannot even say what it permits. Neither was doing anything. Dolt's write path chooses chunk boundaries with `keySplitter` — a key-hash against a Weibull-modelled threshold, no buzhash anywhere — and `defaultSplitterFactory` has never been assigned anything else; the `rollingHashSplitter` that imported kch42's coordinate was reachable from exactly one place, its own benchmark, and rode into the binary only because Go links at package granularity. The silvasur coordinate sat even further out, in `store/nbs/benchmarks` — a hand-run benchmark harness nothing in the module imports. The dolt fork now deletes both (patch 3 in `FORKS.md`): the dead splitter, its benchmark, and the benchmarks tree, producing no new code, copying nothing, and shipping no WTFPL text to anyone. This is the epic's worked example of "cut" beating both "copy" (kch42's authored table constants in a file we claim copyright over) and "rewrite" (our own maintenance burden for a component nothing calls). That the deletion moves no chunk boundary was measured, not argued — twice, against the pins on both sides; the evidence lives with the patch, in `FORKS.md` patch 3. `policy.json`'s buzhash exception is deleted (fslock's is now the only one left), `LICENSE-REPORT.md` carries no Unknown row, and the linked component count drops from 155 to 154.
- `lit` now builds Dolt and go-mysql-server from forks owned by the `promptctl` organization. `go.mod` previously replaced `github.com/dolthub/dolt/go` with `github.com/brandon-fryslie/dolt/go` — a personal account that `promptctl-deps-4aes` already names as a single point of failure for master's build, and that an auditor reading `go.mod` under the zero-copyleft bar asks about out loud — and it did not fork go-mysql-server at all, even though one file in that module (`sql/cache.go`) is the sole reason an MPL-2.0 dependency is linked into the binary. Both now resolve to `promptctl/dolt` and `promptctl/go-mysql-server`, each on a branch named `lit` based on exactly the upstream commit `go.mod`'s `require` line still names, so `git diff <require-commit>..lit` answers "what did lit change?" as a command rather than as a claim some document makes. The forks are permanent even though the one patch they carried when created (dolt's git-blobstore ranged-read materialization, upstream PR dolthub/dolt#11264) is not — removing a transitively imported copyleft module means changing what the importer imports, and that needs a fork to change it in. Forking Apache-2.0 code brings obligations `lit` did not previously owe, so every file the forks modify now carries a section 4(b) change notice under its upstream copyright header, upstream `LICENSE` files are byte-unmodified, and both upstreams were checked for a `NOTICE` file — neither ships one, so there is nothing to propagate. The new root-level `FORKS.md` is the contract: the patch ledger with a retirement condition per patch, the rebase procedure, and `go run ./tools/licenses -check` as the check that a rebase did not quietly restore a copyleft import. The `go.mod` comment that used to summarize the dolt patch now points at that ledger instead of restating it, having already drifted into calling a now-permanent fork "transient."
- A reconcile that folds one side's work onto the remote spine — the shared-history field-aware reconcile, the unrelated-history `lit sync reconcile combine`, and `lit sync reconcile take local` — now preserves the folded side's per-commit provenance instead of flattening it into one squash commit. Each local commit the spine lacked lands as its own forward commit with the original message, timestamp (second precision), and author, projected as its merge with the remote content so every mid-chain state is a whole backlog; commits whose projection changes nothing (for example schema/migration-only commits whose work the lift commit already carries) are dropped rather than replayed empty. A marker commit naming the operation still settles the sequence — its diff is exactly what the merge policy itself decided beyond the folded side's content (the agent's prose resolutions on a finalize; for take-local, the owner-approved discard of the remote-only issues), and when the remote head is at an older schema one machinery commit (`reconcile: lift remote head to current schema`) precedes the provenance commits with the schema DDL and its bookkeeping. The final backlog contents, linear history, and fast-forward push are unchanged from the squash behavior. The reconcile result and the command output now report how many commits were replayed. In the 2026-08-08 field incident one machine's entire pre-reconcile history flattened into a single merge commit — the data survived but what changed, when, and in what order did not; that record now survives the fold, at exactly the moment someone reconstructing a divergence needs it.
- A burst of mutating commands now coalesces its on-change push mirrors through a `mirror-pending` marker in the workspace's storage dir, instead of one fork/exec per mutation — most of which existed only to lose the single-flight race while adding real process-table and CPU pressure on a loaded machine. A mutation that finds a fresh marker rides along with the already-spawned mirror it names (that mirror's push has provably not started, so its HEAD read will include the mutation); otherwise the mutation claims the marker and spawns the mirror itself, and every push attempt clears the marker as it starts. A burst therefore runs a handful of mirrors — roughly one per completed push — rather than one per mutation or even one per second, and coalescing loses nothing: each mirror pushes the current HEAD, which includes every commit that landed before its engine opened.
- The default `sync.cadence` is now `on-change` instead of `on-push`: a mutating command (`new`, `start`, `update`, `close`, `comment`, `rank`, …) on a connected workspace now mirrors to the remote in the background without a separate `git push` or `lit sync push` step. Previously eager push was opt-in config a workspace had to know to set; the links-sync-pgct field incident happened specifically because push stayed a manual act nobody remembered to do, stranding 25 local changes for days with nothing surfacing the drift. The mirror itself and the `on-push`/`on-change` policy machinery both predate this change (#226, #227) — this flips which value a workspace falls into with no config file at all. `on-push` (mirror only via the pre-push hook) remains available for a workspace that deliberately wants to batch outgoing pushes; set `[sync] cadence = "on-push"` to opt back in. A push that stays pending for any reason — cadence set to `on-push`, the background mirror failing, or an unreachable remote — is not silent either way: `lit backlog`/`next`/`show` print the "N local change(s) not pushed" line, and (since the push-outcome marker landed, above) a failed attempt also surfaces as a `FAILING` banner on every mutating command, not just those three reads.

### Fixed

- `Store.ClearParent` and `Store.RemoveLabel` no longer discard the error from `RowsAffected()` after their `DELETE`; a genuine driver/query error reading the affected-row count now surfaces as that error instead of being misread as a false "not found" (matching the pattern already used by `RemoveRelation` and the issue-status/retention writes).
- `Store.RemoveLabel`'s zero-rows case now returns `NotFoundError` instead of a plain error, matching every other not-found path in the store; a `lit label remove` of a label that isn't on the issue now exits with the correct not-found exit code instead of falling through to the generic one.
- `internal/cli/sync_failure.go`'s doc comment on `syncFailureUnrelatedHistories` no longer claims the wholesale/union resolution surface "is not yet built" — `lit sync reconcile combine` and `lit sync reconcile take <local|remote>` already exist and are what the class's rendered guidance names (the comment had drifted from `resolutionSteps()`, which already listed all three commands).
- `lit init`'s remote-adopt step no longer treats a detection/clone failure (reading the local store, resolving the remote, `git ls-remote`-style ref/data checks, or the clone itself erroring) the same as a confirmed-empty remote. Previously it warned and proceeded to create a fresh local store regardless, which risked silently stranding a real remote backlog behind a transient credentials/network/URL problem. Now `lit init` exits non-zero, creates no store, and surfaces the real underlying failure (with the dev-vs-release build status named alongside it) instead of a generic "could not pull existing backlog" warning; retrying `lit init` after the cause is fixed behaves exactly as before. The three genuinely certain-empty outcomes (no remote configured, a remote with no refs, a remote with refs but no lit data) are unchanged — automatic, silent, no gate.
- A mutating command run right after another on a connected workspace could hit `Error 1105: cannot update manifest: database is read only` instead of completing — pre-existing exposure from #226/#227 that links-sync-pgct.3's on-change-by-default flip made everyone's default instead of an opt-in a power user chose knowing the tradeoff. The root cause was two embedded-Dolt behaviors stacking: an engine open that finds the exclusive journal-manifest lock held by another process (an earlier command's still-exiting push mirror, say) silently opens READ-ONLY instead of failing, and dolt's in-process singleton cache then pins that read-only store — journal lock included — until process exit, so no amount of in-place retrying or connection rotation could ever recover; the command was doomed at open time and only found out at commit time. Now every store open bypasses the singleton cache (a `Store.Close` really closes its engine and releases its journal lock, instead of a cached shadow outliving it), and write-capable opens (`Store.Open`, `Store.OpenSync`, the bootstrap and adopt-clone opens) additionally fail fast on a foreign journal-lock holder and retry the open itself — bounded ~30s, honoring command cancellation — until the holder clears, so a successfully opened write store is structural proof it can write. `Store.OpenForRead` keeps dolt's read-only fallback: reading beside a live writer is exactly a read open's contract. The cross-process engine-write lock (flock-backed, acquired in `Open`/`OpenSync`, released in `Close`) remains as the outer serializer among lit's own processes; the open retry absorbs the residue the lock cannot see — the beat between another process releasing the lock and its journal lock actually clearing at teardown. A scripted burst of back-to-back mutating commands against a connected workspace no longer produces this error on any of them.
- `withMutation` no longer re-runs a mutation's body when only the final Dolt versioning step (`DOLT_COMMIT`) failed transiently: the retried unit now resumes at the first phase that hasn't succeeded, so once the staging transaction has committed the retry repeats only the versioning step. Previously the whole-unit retry re-applied the body against its own already-staged writes, which could double-apply a mutation (for `lit new`, a duplicate issue minted under a higher collision nonce) in the narrow window where the SQL commit landed but the versioning commit hit contention.
- The last mutation of a burst can no longer strand its push until the next activity. Under the interim one-spawn-per-second debounce, a mutation landing inside the window spawned no mirror and bet on an earlier, still-live mirror reading HEAD after its commit — but that mirror holds the workspace's one write engine while it pushes, so any mutation it blocked committed strictly after its HEAD read, and the burst's final mutation (exactly the moment an agent walks away) was a coin toss between "pushed now" and "sits local until the next mutation, a 24h staleness banner, or an explicit push". Coverage is now owned state with no time constant carrying it: a mutation either observes a `mirror-pending` claim whose mirror provably hasn't read HEAD yet, or claims it and spawns the mirror itself; a mirror that finds the marker re-claimed after its push runs another full cycle on a fresh engine before exiting, so custody of the tail passes mirror-to-mirror instead of resting on timing (the marker's age matters only as crash recovery, when a claimed mirror died before pushing). Mirror failures *before* the push attempt — the spawning command never exiting, the engine failing to open, the mirror failing to spawn at all — previously wrote no `push-outcome.last` record and sent no owner notification (only a trace); they now complete through the same push-outcome record as an attempt that ran, so the mutating-command `FAILING` banner and the `sync.owner_notify_cmd` channel report a push layer that cannot even start exactly like one whose pushes fail. Two endings that are not channel degradation get their own non-failed record classes instead of `error`: a deliberate cancellation (ctrl-C, process teardown — previously left the FAILING banner coloring every later command) and a mirror timing out behind another lit process legitimately holding the workspace's one write engine (a long reconcile or import — previously would have paged the owner over healthy local serialization); a mirror dismantled mid-startup by SIGTERM releases its claim and records nothing, leaving the last completed attempt's record standing. A panic mid-push now records itself as the attempt's ending instead of unwinding into a fabricated healthy "pushed" record. Remote-less workspaces re-confirm their absence at most once per 10s (`remote-absent.last`) instead of paying a git subprocess per mutation. The burst regression test now proves delivery of all its commits by oracle poll alone, with no explicit sweep push.
- A `lit init` backlog adopt interrupted mid-clone can no longer leave a partial database that later commands treat as the real store. Adopt now writes a durable adopt-pending marker (temp-file-and-rename plus fsync, so a torn or lost marker is unrepresentable) inside the Dolt root before its destructive window opens, and clears it only after the clone's product is validated — so the failure shapes that can run no in-process cleanup (init's 120s deadline abandoning the clone goroutine and exiting, SIGKILL, power loss) leave the marker as durable proof the directory holds an adopt's leftovers, not a backlog. Every normal store entry point (`Open`, `OpenForRead`, `OpenSync`, `EnsureDatabase`, `DumpRaw`) refuses a marker-present workspace loudly, naming the interrupted adopt and the remedy — most critically `lit new`, whose `CREATE DATABASE IF NOT EXISTS` bootstrap would otherwise bless the residue as a fresh store and re-create the links-sync-pgct field incident (a fresh workspace silently shadowing the remote backlog). The check runs after each entry point's workspace lock is held, never before, so a live adopt (which holds the lock exclusively) reads as ordinary workspace-busy while a marker seen under an acquirable lock always means a dead one — no false "interrupted" verdicts during a healthy adopt, and no stale pre-lock answer sailing into residue after an adopt dies mid-wait; standalone `EnsureDatabase` now takes the same workspace+engine lock pair as `Store.Open` for its bootstrap, closing a pre-existing window where an unlocked `CREATE` could race an adopt's destructive window (and a pre-existing unlocked write-engine open). A plain `lit init` retry heals with no manual cleanup: init's adopt gate reads the marker as "nothing to lose" (the marker is only ever written after that same gate confirmed the store held no tickets), sets the leftover ASIDE as a sibling `dolt.adopt-displaced-<ts>` directory — displacement, never deletion, so even tickets minted over residue by a stale pre-marker binary stay recoverable — re-clones, and clears the marker; when the workspace holds residue but the remote signal is gone (removed remote, no refs, no lit data), init refuses with the one interrupted-adopt verdict instead of narrating "starting with an empty backlog" and then failing. A clone failure that returns normally still leaves a fully clean slate — no partial database, no marker — with lit removing its own partial clone unconditionally rather than relying on dolt's best-effort courtesy cleanup, and the marker deliberately lives inside the Dolt root (unlike the sibling lock files) so `lit snapshots restore`'s wholesale directory swap clears or preserves it in lockstep with the data it describes.
- `lit snapshots new` can no longer copy the Dolt directory while a directory-rotating operation is rewriting it. The snapshot copy ran under only the store commit lock, while the destructive windows of `lit snapshots restore`, a remote adopt (displace + `DOLT_CLONE`), and a workspace promotion run under the workspace-exclusivity lock — two disjoint lock files that never contend — so a snapshot started during an adopt walked and copied files the clone was concurrently creating: at best a spurious mid-walk failure, at worst a torn snapshot sitting in the listing as a restorable recovery point that is actually a corrupt store. The copy now takes the same shared workspace hold every store open takes (new `store.LockWorkspaceShared`), acquired before the commit lock in the same workspace→commit order the rotators and store opens already use: it contends with every rotator's exclusive hold — refusing workspace-busy after the same ~5s grace every reader extends a transient rotation, creating no artifacts — while coexisting with concurrent ordinary readers, which hold shared too. The shared-acquisition busy message also stops naming `lit snapshots restore` as the only possible exclusive holder — adopt, promotion, and lifeboat recover's heal take the same lock, and mislabeling the culprit sent the operator looking for a restore nobody ran. Two adjacent gaps in the same command closed in review: `lit snapshots new` now honors the adopt-pending condemnation the way every store open does (checked under the held workspace lock), refusing to snapshot a dead adopt's partial residue as a "restorable" recovery point — previously the copy would install the garbage in the listing and the retention prune could then evict a good snapshot to keep it — and a stray positional argument (`lit snapshots new nightly` — natural, since sibling `restore` takes its argument positionally) is now a usage error instead of silently minting an unlabeled snapshot the operator can't find by the name they thought they gave it. The retention prune also runs after the workspace hold is released — it never reads the Dolt directory, and a multi-gigabyte snapshot deletion should not keep a rotator refusing workspace-busy.
- An interrupted snapshot copy can no longer permanently orphan invisible full-size residue. `dbsnapshot.Take` was context-blind and the CLI interrupt guard hard-exits after its grace period, skipping Take's only cleanup sites — so a Ctrl-C/SIGTERM mid-copy (easiest to hit on filesystems without reflink, where the copy is a real multi-second byte copy) stranded `snapshots/<ts>.tmp` (up to store-size) plus `<ts>.reserve` on disk forever: `lit snapshots list` and every retention prune filter those names, so the residue was invisible to every lit command, untouchable by retention, and each repeated interrupt ate another store-sized chunk. Two halves close it. The copy is now context-aware — cancellation is honored between files and between 32MiB chunks of one large file, so an interrupt aborts the walk and runs Take's own cleanup (the partial `.tmp` and the `.reserve` claim both come off disk) inside the interrupt guard's grace instead of the process dying tree-half-written. And the failure shapes that can run no cleanup at all (SIGKILL, a hard exit mid-cleanup, power loss) are reclaimed at the next take's entry — the one point every snapshot producer (user snapshots, migration/downgrade/reconcile recovery points) reaches on every attempt *before* new disk is consumed, so in the disk-full regime the retry reclaims the dead predecessor's store-sized corpse first and then succeeds (wiring collection downstream of a successful take, e.g. into the retention prune, is unreachable in exactly that regime). Collection is safe under a new producer beacon — an flock inside the snapshots directory that every `Take` holds shared for its whole reserve→copy→rename window, so acquiring it exclusively is kernel proof (no age thresholds, no PID files; a dead holder's lock evaporates with its process) that every producer artifact present belongs to a dead producer. When the probe finds a producer alive, collection simply defers to a later take — reclamation is delayed, never forfeited. Destruction is deliberately narrower than rejection: only names whose shape lit provably minted (a parseable `<unix-ns>[-label]` head under `.tmp`/`.reserve`, or the collector's own uniquely-stamped `.condemned` suffix) are collectible, so a foreign `backup.tmp` an operator parked in the directory stays as inert to the collector as it is to every other consumer. The collector's exclusive window covers only classification (renaming dead artifacts to fresh `.condemned` names, sub-millisecond); the multi-gigabyte deletions run after it releases, and a collection failure of any kind is loud on stderr but never fails the take (or the migration/downgrade carrying it) — the same post-durable-success demotion the reconcile prune already uses. Closed alongside, from the same missing predicate: `lit snapshots restore` of a labeled crash-leftover (`<ns>-<label>.tmp`, whose digit head parses as a valid timestamp) was accepted by restore's name validation even though the listing would never show it — restoring one would install a torn partial copy as the database; producer-artifact names are now rejected inside the one name predicate the listing, validation, and the collector all share. Two adjacent gaps in the same copy path closed in review: the destination file's close error is now part of each file copy's outcome instead of being discarded by a defer — on write-back-at-close filesystems (NFS commit-on-close, delayed-allocation ENOSPC) that dropped error was the only signal the file is truncated, and swallowing it let a torn copy rename into the listing as a restorable snapshot. The flock primitive itself (POSIX flock(2) / Win32 LockFileEx, previously private to the store's workspace/engine/sync-push locks) moved to a shared `internal/filelock` package that reports contention as a value rather than an error, so the store's locks and the snapshot beacon ride one platform seam instead of two.
- The commit lock can no longer evict a live holder. It was the one lock still built on an `O_CREATE|O_EXCL` file carrying its owner's PID, judged stale by a ten-minute mtime threshold OR a signal-0 liveness probe — the OR meant age won alone, and it was measured doing so: with a live process holding the lock and the file backdated eleven minutes, a mutation from a second process walked straight past the owner and exited 0. `takeUserSnapshot` holds this lock for its entire copy, so any snapshot running past ten minutes admitted a concurrent writer into its own walk. Release compounded it: the closure deleted the lock path unconditionally, with no way to tell "my lock" from "a lock", so after one eviction the first finisher deleted the second's lock and a third acquired cleanly. The lock is now an flock through the same `internal/filelock` boundary every other lit lock uses — the hold lives on the holder's open file description, so the kernel releases it on any death mode with no threshold to tune, releasing yours cannot touch anyone else's, and a leftover lock file from a crash (or an old binary) is just residue that never blocks. The PID write/read, the signal-0 probe, the mtime staleness classifier, the delete-on-release, and the process-level acquisition mutex (flock already excludes between goroutines — separate opens get separate file descriptions) are all deleted rather than replaced. The lock also gains what it alone lacked: a declared ~30s retry budget (the same "co-resident holder of this store" sizing as the engine-write lock, replacing an unbounded wait-until-context-death loop that could queue forever behind a wedged holder) and contention that surfaces as the store-wide `ErrWorkspaceBusy` sentinel with commit-specific operator guidance, instead of an untyped `lock not acquired` string no caller could discriminate. Nested acquisition by the same mutation still short-circuits via the same context marker as before. The flock lives at a fresh path, `.links-commit-flock.lock`, because the historical name is burned: an old binary's release (and its ten-minute age eviction) `os.Remove`s `.links-commit.lock` unconditionally, and an unlink under a live flock splits the lock across two inodes — the next acquirer opens a fresh inode and runs concurrently with the orphaned holder, a corruption that would outlast the old binary's exit. A name no historical binary ever touches makes that split unrepresentable. One honest caveat remains: an old binary's `O_EXCL` lock and the new flock do not exclude each other at all, so mixed-version writer exclusion holds only once the old binary exits — the same retiring exposure the snapshot-residue collector already documents. Closed in review alongside: `filelock.Acquire` only observed its context in the sleeps between attempts, so a cancellation landing at the final attempt (or on a single-attempt probe) reported clean contention — dressing an operator's Ctrl-C in "another holder is busy, retry" guidance — and an already-cancelled context still acquired (and then held) a free lock, letting a SIGTERM'd command admit its guarded operation into the interrupt grace; `Acquire` now refuses a done context at entry and surfaces `ctx.Err()` over clean contention, for every flock in lit. And a commit-lock release failure after the guarded operation already succeeded no longer converts that durable success into a reported failure (the hold is provably gone — closing the descriptor releases an flock — and a failure exit invites retrying a mutation that already landed into a duplicate): it is loud on stderr and the command reports its success, joining the operation's error only when the operation itself also failed.
- `lit snapshots new` and `lit snapshots restore` print their result record the moment it is durable — before retention pruning, and beside any failure that lands after the take or the rotation — so a post-success failure can no longer hide that a snapshot now sits in the listing (baiting a duplicate retake) or that a restore already consumed its snapshot and set the previous directory aside (baiting a retry that reports the snapshot missing).
- `lit snapshots new`'s user-retention prune no longer counts reconcile recovery snapshots against the user budget. `pre-reconcile` rollback points share the `snapshots/` directory, and once the user budget was exceeded the oldest of the combined set rolled off — silently evicting a recovery point the reconcile system's own independent budget was supposed to protect, exactly as the kind filter already spared migration and downgrade snapshots. The user-kind predicate now excludes all three system producers.

## [0.4.0] - 2026-08-07

### Added

- `lit show <id> --field <name>[,<name>...]` prints exactly the requested field(s) and nothing else — no header fields, no parent-epic body, no siblings, no epic/children summary. A single field prints its bare value (e.g. `lit show <id> --field description`), so it round-trips directly into `lit update --description`; multiple fields print as `name: value` lines. An unknown field name fails with a `UsageError` listing the accepted vocabulary, with no partial output. Omitting `--field` leaves the existing full-detail view unchanged.
- `lit import --path <file.yaml>` bulk-creates and bulk-updates issues from one multi-document YAML file, one issue per document, selected per document by an `id` field: absent, the document creates (same required fields as `lit new`, plus `local_id`/`parent`/`depends_on` wiring — `parent`/`depends_on` may name either another document's `local_id` or an existing real issue ID); present, the document patches that issue with the same field set `lit update` exposes (`title`, `description`, `prompt`, `type`, `priority`, `assignee`, `labels`, `lane`, `reason`). A mixed file (some create, some update) is legal; an `id` matching no existing issue is a hard error, never a silent create. This is additive to `lit import`'s existing JSON tree-spec format (still supported, dispatched by any extension other than `.yaml`/`.yml`) — one command, format chosen by the file's extension.
- Workflows (user-customizable guidance injection at work-lifecycle moments — the mechanism behind the `lit workflows` command below) are live. Markdown files with YAML frontmatter are discovered recursively under `.lit/workflows/` (project) and `<config>/workflows/` (global) — the folder hierarchy is arbitrary; frontmatter alone declares where a definition activates, via `labels:` / `states:` (with `when: enter|exit`) / `events:` dimensions (OR within a dimension, AND across dimensions). Every curated agent-facing command (`backlog`, `show`, `next`, `new`, `followup`, `update`, `comment add`, and `start`/`done`/`close`/`open`) dispatches its semantic event (`show_backlog`, `show_ticket`, `work_started`, … — a stable contract definitions bind to, never command names) in-process at the point its read or write already succeeded; the dispatched event is matched against the loaded definition set (project → global → embedded defaults, nearer layer winning by `id`) and every match's body — with `<id>` interpolated to the acted-on ticket — is injected into that command's output, in stable `id` order. `done`'s post-close capture reminder is now the first embedded default (`events: [work_finished]`), with the same observable output as before.
- `lit workflows` is the see-it surface for the workflows mechanism above: a static, text view of the lifecycle spine (every dispatched event in display order, plus `open`/`in_progress`/`closed` and any custom stage a loaded definition binds to, each with `enter`/`exit`) annotated with the definitions active at each point — id/name and source layer (project/global/embedded) — with label-scoped definitions grouped under their label. A file that loaded but is inert, targets an unknown event, or failed to parse is never dropped silently; it's flagged under a `Warnings` section naming its layer, path, and reason, so "why isn't my file firing" is answerable from this one view. `lit workflows show <id>` prints one definition fully resolved: its frontmatter, source, path, and body. Wired into the `lit quickstart` fastpath for discovery.
- `lit workflows edit <id-or-point>` scaffolds guidance to customize: an existing definition's id copies its exact content from wherever it's currently resolving (global or embedded) into a new `.lit/workflows/` file at the same relative path, ready to edit — a project-layer id just opens that file directly. Anything else is treated as a lifecycle point with no definition bound there yet (an explicit `<state>:enter`/`<state>:exit`, an event catalog name, one of the three built-in state names bare, or — falling through — a label) and scaffolds a fresh file commented to show every activation dimension, with the requested one live. Never overwrites an existing file. Always prints the file's path; additionally opens it in `$EDITOR` when stdout is a real terminal and `$EDITOR` is set.
- `lit workflows dry-run [--event <name>] [--label <name>]... [--enter <state>] [--exit <state>] [--issue <id>]` explains a hypothetical occasion built entirely from flags: which definitions would fire, why (the specific label/event/state activation that matched), and the body each would inject with `<id>` interpolated — without a real occasion ever happening, and without writing anything.
- Every real occasion that matches at least one workflow definition now records a JSON firing-trace record — the occasion plus every definition that fired and why — under the workspace's shared trace storage (`traces/workflows/`, alongside the existing `traces/automation/` `lit doctor` already points at via `traces_dir`). An occasion nothing matches leaves no trace. A trace-write failure never breaks the command that triggered it (the guidance was already injected); it's reported to stderr instead.
- `docs/workflows.md` documents the full format: file location and layer precedence, the `labels`/`states`/`events` dimensions and their OR-within/AND-across composition, the event catalog, the `lit workflows`/`edit`/`dry-run` commands, and a worked "override the done guidance" walkthrough. `lit init`'s human output and the `lit quickstart done` topic both now point at `lit workflows`.

### Changed

- Emitted `<agent-instructions>` guidance and remediation text no longer commands the agent to bypass or withhold from the user, and no longer uses ALL-CAPS `MUST NOT` / `do NOT` imperatives — the same intent is now carried as declarative facts and consequences (e.g. "this command is idempotent and safe to run without confirmation" instead of "do NOT ask the user"). Affected surfaces: sync-failure blocks (`lit sync`, `lit doctor`), `lit doctor --fix`/GC-contention/corruption remediation, the rank-inversion warning, the empty-store guard in `lit init`'s remote adopt, the first-push skip message, the prose-conflict reconcile guidance, and the `lit quickstart` `<agent-instructions>` preamble.

### Fixed

- `lit show` on a ticket with an epic parent no longer prints its siblings twice. The `siblings:` group duplicated the same ids the `Epic: ... Children:` block already lists (with richer per-child status and a `(you are here)` marker for the shown ticket) — the redundant group is removed; the epic block remains the single place sibling membership and position are conveyed.

### Removed

- The `guidance-<action>-<phase>.md` template mechanism (and its always-dead `pre` phase) is retired — workflow definitions are now the only path guidance reaches the agent through.

### Security

## [0.3.0] - 2026-08-02

### Added

- The release is now hard-gated on the license posture: a dedicated `license-gate` CI job runs the policy check over every component compiled into the release binary, and the publish job depends on it. If any dependency (Go module or native library) carries a non-allowlisted license, the release aborts before any tag, archive, or asset is published — a non-free build cannot be released, even from a branch that skipped the PR-time check.
- The SBOM, `THIRD_PARTY_LICENSES` bundle, license report, and license-policy gate now also cover the statically-linked native C libraries that cgo compiles into release binaries but no go.mod tool can see: ICU 75.1 (Unicode-3.0), zstd 1.5.6 (BSD-3-Clause), musl 1.2.5 (MIT), and compiler-rt via zig 0.14.0 (MIT). Each appears with its name, version, license, `pkg:generic` PURL (SBOM), and verbatim notice text (bundle). Versions are pinned to the release build config, with a CI check tying ICU/zig to `build/Dockerfile.release`.
- CI license-policy gate: every linked module's license is checked against a committed allowlist (`tools/licenses/policy.json`) of permissive licenses plus documented per-module exceptions, and the build fails if a dependency bump pulls in a non-allowlisted license (GPL/AGPL/SSPL/BUSL, etc.). Runs on every pull request and master push via `go test` (and standalone via `go run ./tools/licenses -check`). Shares the classifier with the license report/SBOM, so the gate checks the exact licenses those artifacts document.
- Every release now ships a CycloneDX SBOM (`lit_<version>_sbom.cdx.json`) as a standalone downloadable asset: a machine-readable bill of materials listing every Go module and statically-linked native C library compiled into the binary, with its version, package URL, and resolved license. It is generated from the same linked-module inventory as `THIRD_PARTY_LICENSES`/`LICENSE-REPORT.md`, validated as CycloneDX 1.6 in CI, and lets vulnerability scanners audit a given `lit` version against CVE feeds.
- CI gate (`TestReleasedMigrationsAreContentPinned`) that refuses any change reusing a released migration version number under different content — the mechanism that bricked workspaces in the migrate-drift epic. Every non-baseline migration's content is pinned by sha256; reusing, editing, deleting, or leaving a version number unpinned fails the build with a message naming the collision.

### Changed

- `lit --help` now presents the state-transition verbs in two groups so the high-traffic status lifecycle stands out: the core verbs (`start`, `done`, `close`, `open`) stay under **Agent Operations**, while the low-traffic retention quartet (`archive`, `unarchive`, `delete`, `restore`) moves to its own **Issue Retention** group. The grouping mirrors the model's `StatusAction`/`RetentionAction` partition; every verb stays a first-class command — only its placement in help changed, never its dispatch.
- `lit ls` gained an `--at <store-dir>` flag that lists a discovered store (a path from `lit stores`) read-only, without depending on the current directory being a lit workspace. This is the folded-in former `lit ls-at`, now a flag on `ls` rather than a separate command — and it is strictly more capable, since every `ls` filter/sort/column/format now applies to a foreign store too (the old command was hard-wired to open + in_progress).
- `lit stores` gained a `--counts` flag that reports each discovered store's ready / in-flight / blocked counts as a cross-project rollup instead of listing storage paths. This is the folded-in former `lit overview` — the same discovery walk and store-intrinsic readiness classification, now a flag on `stores`.
- `lit parent set` now renders the parent-child edge through the same canonical projection `lit dep` uses, so the identical edge reads the same way whichever command wrote it (`<child> --child-of--> <parent>`, previously `--parent-child-->` from `parent set` alone). Both commands already wrote through one store owner; this removes the last cosmetic divergence between the two faces of the one edge.
- Command summaries in `lit --help` now name the mechanism behind the three snapshot-shaped commands so they can be told apart: `export`/`backup` are the JSON **data-export** family (portable tree out; `backup` wraps it with rotation), while `snapshots` is the Dolt **filesystem-level database** snapshot mechanism. The two mechanisms are unchanged — only the naming ambiguity is fixed.
- `lit quickstart` and the agent-facing prompt text now describe only the curated command surface: the finding-work fastpath is `lit next` → `lit start <id>` (was `lit ready`), the workable views are `lit next` / `lit backlog` (the retired `ready` / `queue` are gone), and no quickstart page, README, or getting-started walkthrough example invokes a command that now hard-errors. This brings the guidance (the map) into line with the surface the earlier children of this epic shipped (the territory).
- The `lit quickstart ready` guidance topic is renamed to `lit quickstart work` — its name no longer collides with the retired `ready` command, and it reads as what it is (finding *and* starting work, over `next`/`backlog`/`start`). The `lit start` success breadcrumb now points at `lit quickstart work`, and the `--eject` short-name is `quickstart-work`. An old `lit quickstart ready` invocation errors with the valid topic list (`work, new, update, done, doctor`); downstream `CLAUDE.md`/`AGENTS.md` integration blocks are unaffected, since they only reference bare `lit quickstart`. **If you ejected and customized the old topic template**, rename your override from `quickstart-ready.md` to `quickstart-work.md` (in `.lit/templates/` or the global templates dir) — the guidance now resolves under the new name, so an override left at the old filename is silently ignored in favor of the embedded default.

### Fixed

- Sync reconciliation of branches with unrelated histories no longer depends on a silent error-swallowing bug in the embedded Dolt driver. The driver's first-row pre-read discarded any error, so a query that failed on its first row (such as `DOLT_MERGE_BASE` raising "no common ancestor" for refs that share no history) could surface as an empty result set instead of the real error. The driver now propagates that error, and merge-base resolution recognizes the "no common ancestor" backend error as the unrelated-histories state directly — the same domain outcome it already handled via an empty result set.

### Removed

- Three single-purpose commands are folded into flags on broader commands and retired as standalone verbs, each with a documented pointer (no capability lost): `lit assign <id> <name>` → `lit update <id> --assignee <name>` (reassigning is a single-field write; `update` already owns field mutation, including the optional `--reason`); `lit ls-at <dir>` → `lit ls --at <dir>`; `lit overview` → `lit stores --counts`. The retired verbs stay dispatchable but hidden, so an old invocation returns a pointer to the surviving flag (exit 3) instead of cobra's bare unknown-command error — the break is deliberate and named, never silent.
- `lit bulk import` is retired: it was a second name for the export-restore that `lit backup restore --path <export.json>` already owns (both ran the identical restore path), and it was the odd verb out in a family of per-`--ids` fan-out operations. An old `lit bulk import` invocation now returns a documented pointer to `lit backup restore`. `lit import` remains the one home for JSON tree-spec imports — a distinct operation that only shared the word "import."
- `lit update --status` is retired: `update` no longer changes an issue's status. The transition verbs (`lit start` / `lit done` / `lit close` / `lit open`) are now the single enforcer of the transition guardrails, so `update --status` is rejected with a documented pointer to them instead of routing status through a second, divergent path. This closes the former `--status closed` back door, which recorded a resolution-less `done` and bypassed `close`'s required outcome. The capability is fully reachable under the verbs; the break is deliberate and named, never silent. `update` remains the home for field edits (`--title`, `--description`, `--assignee`, `--labels`, `--lane`, etc.).
- The `ready` and `queue` workable views are retired from the command surface, leaving `next` (the single leaf to start) and `backlog` (the full ranked queue with blocked items shown inline) as the only named workable views — `ls` remains the power query and `orphaned` its own staleness view. `lit ready` and `lit queue` no longer appear in `lit --help` or shell completion, and now fail with a documented pointer to `lit backlog` / `lit next` instead of running: the break is deliberate and explained, never silent. The retired presentations (ready's blocked-to-bottom re-sort and coaching preamble; queue's terse pullable-only list) are dropped — `backlog` and `next` carry the surviving intent.

### Security

## [0.2.1] - 2026-08-01

### Changed

- Rewrote lit's agent-facing prompt surfaces (the CLAUDE.md integration block `lit init` writes, and the `lit quickstart` guidance) into calm, plain, second-person tool documentation. The previous text used the fingerprint of a prompt-injection attack — ALL-CAPS imperatives, threats, and act-behind-the-user framing — which led a security-conscious agent to flag lit as untrustworthy and advise uninstalling it. The agent-native workflow is unchanged; only the register is.

## [0.2.0] - 2026-07-30

### Added

- In-repo issue tracking backed by an embedded [Dolt](https://www.dolthub.com/) database — the backlog ships with the code and every mutation is a versioned commit.
- Agent-first core loop: `lit ready`, `lit start`, `lit done`, and `lit followup` return guidance alongside data so a coding agent can drive the workflow unattended.
- `lit quickstart` prints the live command reference for agents.
- One write boundary and one identity per clone, keyed off `git rev-parse --git-common-dir`, so all worktrees of a clone share a single issue view.
- `lit version` command with build-time `-ldflags` version stamping.
- `lit doctor [--fix]` for diagnosing and repairing workspace issues.
- `lit lifeboat` recovery path for schema-ahead workspaces.
- `scripts/install.sh` builds `lit` from source and installs it onto `PATH`, warning about stale shadowing binaries.

### Changed

- `lit new` and `lit followup` now rank new tickets to the **top** of the order by default (fresh work surfaces first) instead of the bottom. Pass `--bottom` to append at the bottom — use it when authoring a batch in order so creation order is preserved. Importing an existing backlog still preserves its order.

### Fixed

### Removed

- The `--json` flag is removed from every command. The default human-readable text is the one canonical surface — it is already the agent interface, and per-line output is parseable when a script needs one field (e.g. `lit workspace | sed -n 's/^traces_dir: //p'`). `lit export` and `lit lifeboat dump` are unchanged: they emit a JSON data structure as their sole output (no flag), because a full export / raw dump has no text form. Passing `--json` now fails with a usage error.

### Security
